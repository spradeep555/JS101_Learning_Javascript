var initialUsername = "Pradeep";
var initialPassword = "cat143";

var username = "pradeep";
var password = "cat144";

if (initialUsername == username) {
  if (initialPassword == password) {
    console.log("log in");
  }
  else {
    console.log("Password missmatch");
  }
}
else {
  console.log("Username missmatch");
}